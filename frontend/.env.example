# Copy this file to ".env" and adjust values as needed:
#   cp .env.example .env
# Note: Vite only exposes variables prefixed with VITE_, and they are
# bundled into the browser code (PUBLIC) — never put secrets here.

# Address of the FastAPI backend.
VITE_API_URL=http://localhost:8000
